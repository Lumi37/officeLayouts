// import {selectedOffice, svgContainer} from '../index.js'

//  const svgContainer2 = document.querySelector("#svgs");


// svgContainer2.addEventListener('mouseover',e=>{
//     if(selectedOffice && e.target.dataset.id ){
//         const selectedOfficeUsers = document.querySelectorAll('.chosenOffice rect[data-id]')
//         const tooltip = document.createElement('div')
//         tooltip.innerText = `${e.target.dataset.user},${e.target.dataset.outlet}`
//         tooltip.classList.add('tooltip')
//         document.appendChild(tooltip)

//         const rectBounds = e.target.getBoundingClientRect()
//         tooltip.style.top = rectBounds.top + 'px'
//         tooltip.style.left = rectBounds.right + 'px'
//     }
// })
